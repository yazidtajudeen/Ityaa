allprojects {
    repositories {
        google()
        mavenCentral()
    }

    tasks.withType(JavaCompile) {
        options.fork = true
        options.forkOptions.executable = "/Users/student/Downloads/jdk-21.0.5.jdk/Contents/Home/bin/javac"
    }
}

// Make sure the Android Gradle plugin is compatible with Java 21
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.1'  // Ensure this is compatible with Java 21
    }
}
